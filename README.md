#28. Lambda. Stream API
#Описание задания
Добавьте в шагомер метод `Stream<Integer> getAllAbove(int steps)`, который возвращает стрим из всех дней, когда было больше чем steps шагов. Тесты на это можно не писать